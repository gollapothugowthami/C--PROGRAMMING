#include <stdio.h>
int main() {
int a, b;
scanf("%d %d", &a, &b);
if(num % 3 == 0 && num % 7 == 0)
{
  printf("multiply a,b");  
} 
else if
{
    printf("dont multiply a,b");
}    

    
    return 0;
}
