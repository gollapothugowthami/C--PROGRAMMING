#include <stdio.h>
int main() {
    int  sharing type;
    scanf("%d", &hostel fee);
    switch (hostel fee){
     case 1:
     if(room = ₹80000){
     printf("single");}

     break;
     case 2:
     else if(room = ₹60000){
    printf("double");}
     break;
     case 3:
     else(room =₹45000){
printf("triple");
     }

    
     break;
     defalut:
     printf("invalid hostel fee");
       
    }
     return 0;
}
printf("hostel ₹%d", hostel fee);
return 0;
}
