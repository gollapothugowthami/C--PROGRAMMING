#include <stdio.h>
int main() {
int a, b, c;
a = 10;
printf("The value of a BEFORE increment is %d", a);    
 /* increment A */
 printf("The value of a AFTER increment is %d", a); 
 S = a-1;
 printf("The value of a AFTER increment is %d", a);  
    
    return 0;
}