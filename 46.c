#include <stdio.h>
int main() {
    char str[100];
    int length =o;
    printf("enter a string: ");
    fgets(str, sizeof(str),stdin);
     while(str[length] !='\o' ) {
 fgets   
    if (str[length] =='\n'){
        break;
    }
    length++;
 }
    printf("length of the string = %d\n", length);
    
    
    return 0;
}