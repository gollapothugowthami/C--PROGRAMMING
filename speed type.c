#include <stdio.h>
int main() {
int order amount, speed type;
scanf("%d %d", &order amount, & speed type);
switch (speed type){
 case 1:
 if(normal = 50)
 {
    printf("normal");
 }
 break;
 case 2:
 if(order <= 100)
 {
    printf("100");
 }
 break;
 case 3:
 else(order >= 1000)
 {
    printf("free");
 }
 break;
 
      return 0;
}
