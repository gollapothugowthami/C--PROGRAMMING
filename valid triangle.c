#include <stdio.h>
int main() {
 int a, b, c;
 scanf("%d %d %d", &a, &b, &c);
 sum = a+b+c= 180
 if(a+b+c)
 {
    printf("valid triangle");
 }
 else if
 {
    printf("not an valid tiangle");
 }
 return 0;
}
