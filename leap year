#include <stdio.h>
int main() {
int year;
scanf("%d", &year);
printf("enter the year:");
if(year%400==0)
{
    printf("it is leap year");
}
    else
    {
        printf("it is not a leap year");
    }
    return 0;
}
